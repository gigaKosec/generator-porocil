/* import storage_dummy from "./storage";
if (nastavitev){
  import {loadDays, storeDays} from storage_dummy;
}
else {
  import {loadDays, storeDays} from storageOnServer;
}

export default {
  loadDays,
  storeDays
} */